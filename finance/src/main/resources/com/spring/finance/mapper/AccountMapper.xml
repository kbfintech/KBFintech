<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.finance.mapper.AccountMapper">
	
	<select id="getAccountNum" parameterType="String" resultType="String">
		SELECT ACCOUNT_NUMBER FROM ACCOUNT WHERE M_ID = #{M_ID}
	</select>
	
	<insert id="regitAccount" parameterType="com.spring.finance.domain.AccountVO">
		INSERT INTO ACCOUNT(M_ID, M_NAME, ACCOUNT_NUMBER, PHONE, ACCOUNT_MONEY) VALUES(#{M_ID}, #{M_NAME}, #{ACCOUNT_NUMBER}, #{PHONE}, #{ACCOUNT_MONEY})
	</insert>
	
	<update id="updateAccount" parameterType="com.spring.finance.domain.AccountVO">
		UPDATE ACCOUNT SET M_NAME = #{M_NAME}, ACCOUNT_NUMBER = #{ACCOUNT_NUMBER}, PHONE = #{PHONE}, ACCOUNT_MONEY = #{ACCOUNT_MONEY} WHERE M_ID = #{M_ID}
	</update>
	
	<select id="getAccount" parameterType="String" resultType="com.spring.finance.domain.AccountVO">
		select * from ACCOUNT where M_ID = #{M_ID}
	</select>
		
	<select id="getAccountMoney" parameterType="String" resultType="int">
		select ACCOUNT_MONEY from ACCOUNT where M_ID = #{M_ID}
	</select>
	
	<update id="updateTransferAccount" parameterType="com.spring.finance.domain.TransferVO">
		update account set ACCOUNT_MONEY = ACCOUNT_MONEY - #{TRANSFER_MONEY} where M_ID = #{M_ID}
	</update>
</mapper>